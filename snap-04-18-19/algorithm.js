i=1;
while (i=2){
	if i * 453===0}
	output 'grams';
}


if weight is in pounds
multiply by 453
total is poound converted to grams
output equeal number1 * 453


Given weightInPounds
Set intermediaryVariable to weightPounds * 1000
return weightGrams

// correction (right way)
function convertToGrams (weight, units) {

	if (units === "lbs") {
		return weight * 453.592;
	} else if (units === "kg") {
		return weigbt * 1000;
	} else if (units === "mg") {
		return weight / 1000;
	} else if (units === "g") {
		 return weight;
	}

number:
i:
sum:

function sumFactors(number) {
	sum = 0;
	for (i = number; i >0 ; i--) {
		if (number % i === 0) {
			sum = i + sum;
		}
	}
	return sum;
}

console.log(sumFactors(number 1));
console.log(sumFactors(number 4));
console.log(sumFactors(number 12));
console.log(sumFactors(number 178967923));


